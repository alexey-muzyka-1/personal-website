---
# Секретов здесь нет и быть не должно: файл лежит в репозитории.
# Токен бота, секрет webhook и пароль базы живут только в deploy/.env
# на самой машине. Ansible их не возит и не знает.
deploy_user: funnel
app_dir: /opt/funnel
bot_domain: bot.alexeymuzyka.com
