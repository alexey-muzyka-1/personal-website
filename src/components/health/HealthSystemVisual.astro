<figure class="system-figure" aria-labelledby="system-caption">
  <div class="system-canvas">
    <div class="system-node node-watch">
      <span class="node-kicker">ФАКТ</span>
      <strong>Apple Watch</strong>
      <small>пульс · дистанция · время</small>
    </div>
    <span class="system-arrow arrow-one" aria-hidden="true">→</span>
    <div class="system-node node-import">
      <span class="node-kicker">МОСТ</span>
      <strong>Экспорт</strong>
      <small>XML → чистые сводки</small>
    </div>
    <span class="system-arrow arrow-two" aria-hidden="true">→</span>
    <div class="system-node node-vault">
      <span class="node-kicker">КОНТЕКСТ</span>
      <strong>Obsidian</strong>
      <small>план · факт · самочувствие</small>
    </div>
    <span class="system-arrow arrow-three" aria-hidden="true">→</span>
    <div class="system-node node-agent">
      <span class="node-kicker">РЕШЕНИЕ</span>
      <strong>AI-агент</strong>
      <small>сводка · вопросы · новый план</small>
    </div>
    <div class="loop" aria-hidden="true">
      <span>следующая тренировка</span>
      <i>↩</i>
    </div>
  </div>
  <figcaption id="system-caption">Устройство измеряет. Obsidian хранит контекст. Агент соединяет их перед следующим решением.</figcaption>
</figure>

<style>
  .system-figure {
    margin: 34px 0;
  }
  .system-canvas {
    position: relative;
    display: grid;
    grid-template-columns: 1fr auto 1fr auto 1fr auto 1fr;
    align-items: stretch;
    gap: 10px;
    padding: 34px 28px 62px;
    border: 1px solid var(--line);
    border-radius: 18px;
    background:
      radial-gradient(circle at 8% 10%, color-mix(in srgb, #6f8f72 12%, transparent), transparent 28%),
      radial-gradient(circle at 92% 90%, color-mix(in srgb, #90755f 12%, transparent), transparent 32%),
      color-mix(in srgb, var(--fg) 2%, var(--bg));
    overflow: hidden;
  }
  .system-node {
    min-height: 134px;
    padding: 18px 16px;
    border: 1px solid var(--line);
    border-radius: 13px;
    background: color-mix(in srgb, var(--bg) 94%, transparent);
    display: flex;
    flex-direction: column;
    justify-content: center;
    box-shadow: 0 10px 32px color-mix(in srgb, var(--fg) 5%, transparent);
  }
  .system-node strong {
    font-family: var(--serif);
    font-size: 1.05rem;
    font-weight: 500;
    margin: 7px 0 5px;
  }
  .system-node small {
    color: var(--muted);
    line-height: 1.35;
  }
  .node-kicker {
    color: var(--accent);
    font-size: 0.62rem;
    font-weight: 600;
    letter-spacing: 0.12em;
  }
  .system-arrow {
    display: grid;
    place-items: center;
    color: var(--muted);
  }
  .loop {
    position: absolute;
    left: 20%;
    right: 7%;
    bottom: 20px;
    border-bottom: 1px solid var(--line);
    height: 24px;
    color: var(--muted);
    font-size: 0.7rem;
    letter-spacing: 0.04em;
    text-align: center;
  }
  .loop span {
    background: var(--bg);
    padding: 0 9px;
  }
  .loop i {
    position: absolute;
    left: -13px;
    bottom: -9px;
    font-family: var(--serif);
    font-size: 1.2rem;
    font-style: normal;
  }
  figcaption {
    margin-top: 10px;
    color: var(--muted);
    font-size: 0.8rem;
    line-height: 1.5;
  }
  @media (max-width: 720px) {
    .system-canvas {
      grid-template-columns: 1fr;
      padding: 22px 18px;
      gap: 8px;
    }
    .system-node { min-height: 105px; }
    .system-arrow { transform: rotate(90deg); height: 18px; }
    .loop { display: none; }
  }
</style>
