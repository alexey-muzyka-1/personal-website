const base = import.meta.env.BASE_URL.replace(/\/?$/, '/');

// Подборки книг. Порядок здесь = порядок вкладок на странице.
//
// id совпадает со значением category в .md книги, поэтому завести новую
// подборку — это строка здесь плюс страница на два десятка символов.
// Раньше подборка означала ещё и копию всей вёрстки: тексты в шапке
// разъезжались между вкладками, потому что править их надо было в
// нескольких файлах сразу.
export const bookCategories = [
  { id: 'hudozhestvennaya', label: 'Художественная', href: `${base}books` },
  { id: 'biznes', label: 'Бизнес', href: `${base}books/business` },
  { id: 'pokolenie', label: 'Новое поколение', href: `${base}books/pokolenie` },
] as const;

export type BookCategory = (typeof bookCategories)[number]['id'];

// Что это за страница. Один текст на все вкладки: подборки разные, а
// причина собирать их — одна.
export const booksLede =
  'Не рекомендательный список. Попытка понять, какие книги и насколько на меня повлияли: ' +
  'собираю их здесь и дополняю, когда вспоминаю или дочитываю.';
