const base = import.meta.env.BASE_URL.replace(/\/?$/, '/');

// Что отдаю целиком: артефакты, а не тексты.
//
// Статьи здесь не повторяются. Их читают, и для этого наверху есть
// развилка «что почитать». Сюда попадает то, что скачивают или клонируют
// и запускают у себя. Разница не формальная: у строки должен быть глагол
// «забрать», а не «прочитать».
//
// Правило то же, что у инструментов: только то, что уже работает у меня
// каждый день. Ничего не заводим ради строчки в таблице.
export const giveaways = [
  {
    name: 'контент-агент',
    kind: 'агент',
    what: 'пишет посты моим голосом, от разбора чужого видео до текста',
    href: `${base}tools/content-agent`,
  },
  {
    name: 'health-vault',
    kind: 'структура',
    what: 'пустой Obsidian-vault и промпт для агента, забирается архивом',
    href: `${base}articles/digital-health`,
  },
];
